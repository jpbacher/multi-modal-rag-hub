boto3>=1.26.0            # to read from S3
openai>=1.86.0           # for LLM calls
langchain>=0.3.25        # chaining & prompts
chromadb>=1.0.12         # vector store
pypdf>=5.6.0             # PDF loading (if you ingest PDFs)
python-dotenv>=1.1.0     # for reading .env in Lambda
